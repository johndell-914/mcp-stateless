# Private planning materials — never ship (contain NDA references)
/docs/

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
*.egg-info/
build/
dist/

# Env / secrets
.env
.env.*
!.env.example

# Local editor / assistant state
.claude/

# Scratch / build artifacts — anchored to root (/_* does NOT match src/**/__init__.py)
/_*
